@* RedirectTo — tiny helper to navigate programmatically from markup *@
@inject NavigationManager Nav

@code {
    [Parameter, EditorRequired] public string Path { get; set; } = "/";

    protected override void OnInitialized() => Nav.NavigateTo(Path, replace: true);
}
